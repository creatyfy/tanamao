workflows:
  ios-release:
    name: iOS Release Build
    max_build_duration: 120
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: Apple Codemagic
    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.tanamaoc.delivery
      vars:
        XCODE_PROJECT: "ios/App/App.xcodeproj"
        XCODE_SCHEME: "App"
      node: 22.13.0
    scripts:
      - name: Install and build
        script: |
          set -euo pipefail
          node --version
          npm --version
          
          # Instala dependências com NPM
          npm install
          
          # Executa o build com NPM
          npm run build
          
          echo "Verificando pasta de build..."
          if [ -d "dist" ]; then
            echo "Pasta dist encontrada com sucesso."
            ls -la dist/
          else
            echo "ERRO: Pasta dist não encontrada após npm run build."
            ls -la
            exit 1
          fi
      - name: Setup iOS platform
        script: |
          set -euo pipefail
          rm -rf ios
          npx cap add ios
          cd ios/App
          xcodebuild -resolvePackageDependencies \
            -project App.xcodeproj \
            -scheme App
          cd ../..
          npx cap sync ios
          if [ ! -d "$XCODE_PROJECT" ]; then
            echo "ERRO: Projeto Xcode não encontrado"
            exit 1
          fi
          echo "OK: $XCODE_PROJECT"

      - name: Generate app icons
        script: |
          # Garante que a logo correta esteja nos locais esperados pelo gerador
          mkdir -p assets
          cp logo_new.png assets/logo.png
          cp logo_new.png assets/icon-only.png
          cp logo_new.png assets/splash.png
          cp logo_new.png assets/splash-dark.png
          # Gera os assets para iOS
          npx @capacitor/assets generate --ios
      - name: Resolve Swift Package Manager dependencies
        script: |
          xcodebuild -resolvePackageDependencies \
            -project "$XCODE_PROJECT" \
            -scheme "$XCODE_SCHEME" \
            -destination "generic/platform=iOS"
      - name: Increment iOS build number
        script: |
          PLIST="ios/App/App/Info.plist"
          /usr/libexec/PlistBuddy -c "Set :CFBundleVersion $BUILD_NUMBER" "$PLIST"
          /usr/libexec/PlistBuddy -c "Set :CFBundleShortVersionString 1.0.1" "$PLIST"
          /usr/libexec/PlistBuddy -c "Add :ITSAppUsesNonExemptEncryption bool false" "$PLIST" 2>/dev/null || \
          /usr/libexec/PlistBuddy -c "Set :ITSAppUsesNonExemptEncryption false" "$PLIST"
          /usr/libexec/PlistBuddy -c "Set :CFBundleDisplayName Tá Na Mão" "$PLIST" 2>/dev/null || \
          /usr/libexec/PlistBuddy -c "Add :CFBundleDisplayName string Tá Na Mão" "$PLIST"
          /usr/libexec/PlistBuddy -c "Set :NSCameraUsageDescription 'Usado para tirar foto de perfil da loja ou do motoboy'" "$PLIST" 2>/dev/null || \
          /usr/libexec/PlistBuddy -c "Add :NSCameraUsageDescription string 'Usado para tirar foto de perfil da loja ou do motoboy'" "$PLIST"
          /usr/libexec/PlistBuddy -c "Set :NSPhotoLibraryUsageDescription 'Usado para selecionar foto de perfil da loja, motoboy ou produto'" "$PLIST" 2>/dev/null || \
          /usr/libexec/PlistBuddy -c "Add :NSPhotoLibraryUsageDescription string 'Usado para selecionar foto de perfil da loja, motoboy ou produto'" "$PLIST"
          /usr/libexec/PlistBuddy -c "Set :NSPhotoLibraryAddUsageDescription 'Usado para salvar fotos do app'" "$PLIST" 2>/dev/null || \
          /usr/libexec/PlistBuddy -c "Add :NSPhotoLibraryAddUsageDescription string 'Usado para salvar fotos do app'" "$PLIST"
      - name: Set up code signing settings on Xcode project
        script: |
          xcode-project use-profiles
      - name: Build ipa for distribution
        script: |
          xcode-project build-ipa \
            --project "$XCODE_PROJECT" \
            --scheme "$XCODE_SCHEME"
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
        beta_groups:
          - Internal Testers

  android-release:
    name: Android Release Build
    max_build_duration: 60
    instance_type: mac_mini_m2
    environment:

      vars:
        PACKAGE_NAME: "com.tanamaoc.delivery"
      node: 22.13.0
      java: 21
    scripts:
      - name: Install and build
        script: |
          set -euo pipefail
          npm install
          npm run build
      - name: Increment Android build number
        script: |
          cd android
          # Incrementa versionCode
          sed -i '' 's/versionCode [0-9]*/versionCode 3/' app/build.gradle
          # Incrementa versionName
          sed -i '' 's/versionName "[0-9]*\.[0-9]*"/versionName "2.1"/' app/build.gradle
          cd ..
      - name: Setup Android platform
        script: |
          set -euo pipefail
          # Remove a plataforma Android para garantir um ambiente limpo
          rm -rf android
          npx cap add android
          npx cap sync android
          # Limpa os diretórios de ícones existentes para evitar conflitos
          rm -rf android/app/src/main/res/mipmap-*
      - name: Generate Android icons
        script: |
          set -euo pipefail
          # Garante que a logo correta esteja nos locais esperados pelo gerador
          mkdir -p assets
          cp logo_new.png assets/logo.png
          cp logo_new.png assets/icon-only.png
          cp logo_new.png assets/splash.png
          cp logo_new.png assets/splash-dark.png
          # Gera os assets para Android
          npx @capacitor/assets generate --android
      - name: Build Android AAB
        script: |
          # Limpa o cache do Gradle para evitar corrupção
          rm -rf android/.gradle
          # Remove o wrapper corrompido
          rm -rf android/gradle/wrapper/gradle-wrapper.jar
          
          # Entra na pasta android para recriar o wrapper e executar o build
          cd android
          # Recria o Gradle Wrapper (se necessário)
          gradle wrapper
          # Garante permissão de execução para o gradlew recém-criado/baixado
          chmod +x gradlew
          cd ..
          
          # Verifica se o keystore existe antes de mover
          if [ -f "tanamao-release.keystore" ]; then
            echo "Keystore encontrado. Movendo para android/app..."
            mv tanamao-release.keystore android/app/tanamao-release.keystore
          else
            echo "AVISO: tanamao-release.keystore NÃO ENCONTRADO na raiz do projeto."
            echo "O build vai falhar se o keystore não estiver em android/app/tanamao-release.keystore"
          fi
          
          # Entra na pasta android e executa o build
          cd android
          ./gradlew bundleRelease
          cd ..

          # Assinatura manual do AAB com jarsigner
          echo "Realizando assinatura manual do AAB com jarsigner..."
          jarsigner -verbose -sigalg SHA1withRSA -digestalg SHA1 \
            -keystore tanamao-release.keystore \
            -storepass tanamao123 \
            android/app/build/outputs/bundle/release/app-release.aab \
            tanamao

          # Otimização do AAB com zipalign (opcional, mas recomendado)
          echo "Otimizando AAB com zipalign..."
          # Encontra o zipalign no Android SDK
          ZIPALIGN_PATH=$(find $ANDROID_SDK_ROOT -name "zipalign" | head -n 1)
          if [ -z "$ZIPALIGN_PATH" ]; then
            echo "ERRO: zipalign não encontrado. Certifique-se de que o Android SDK Build-Tools está instalado."
            exit 1
          fi
          $ZIPALIGN_PATH -f -p 4 \
            android/app/build/outputs/bundle/release/app-release.aab \
            android/app/build/outputs/bundle/release/app-release-signed.aab
          
          # Renomeia o arquivo assinado para o nome original esperado
          mv android/app/build/outputs/bundle/release/app-release-signed.aab \
            android/app/build/outputs/bundle/release/app-release.aab
    artifacts:
      - android/app/build/outputs/bundle/release/*.aab
